========================================
论文讨论记录
========================================

【日期】2026年4月12日

【议题1】元路径方案对初版意图提取的替代程度

【结论】采用融合方案（方案B）——保留LLM专家意图提取 + 元路径结构意图增强

【核心理由】
1. 两者捕获不同层面的意图信息，互补而非替代：
   - LLM专家投票 → 语义层面意图（内容驱动），基于单篇新闻内容即可判断
   - 元路径学习 → 结构层面意图（关系驱动），从图的拓扑结构中隐式学习

2. 单独用元路径的弱点：依赖图的规模和质量，数据稀疏时元路径会退化
3. 单独用LLM专家的弱点：成本高、可能有幻觉，缺乏数据驱动的验证
4. 融合后可互相印证：LLM给出语义意图判断，元路径提供结构证据支撑

【最终意图表示】
  I_final = Fusion(I_LLM, I_meta-path)
  即：粗粒度语义意图（LLM）+ 细粒度结构意图（元路径）的跨粒度融合

【论文故事线】
  "我们既利用LLM的语义理解能力显式推理意图，又通过元路径从图结构中隐式学习意图模式，两者互补融合。"

【创新点】跨粒度意图融合机制

【待优化】初版7位专家可精简为3-4个核心视角，降低成本

========================================

【议题2】DHGAT是否保留 & 图神经网络层的设计

【结论】采用混合架构，统一使用"多头注意力 + 稀疏正则化"机制

【最终架构（三层流水线）】
  第一层（元路径意图提取）：
    5条元路径 → 各自构建同构子图 → 各跑一个GCN
    → 5头注意力（软融合）→ 意图向量 I_meta

  第二层（异构图聚合）：
    4种边类型（评论边、意图边、相似边、点赞边）→ 各自邻居聚合
    → 多头注意力（软融合 + 稀疏约束）→ 最终节点表示

  第三层：分类

【关键设计决策】放弃Gumbel-Softmax硬选择，全部改为软注意力 + 稀疏约束

【理由】
1. 硬选择 vs 软融合的判断标准：
   - 同质可替代的选项 → 适合硬选择（隔绝噪音）
   - 异质互补的选项 → 适合软融合（各取所长）
2. 但现实中边界模糊，不如统一方案：全部用软注意力 + 稀疏正则化
3. 稀疏约束让模型自己学会"谁重要谁不重要"：
   - 如果某种边/元路径确实是噪音 → 权重自动趋近0（等效硬选择）
   - 如果有一点点用 → 模型也不会完全丢弃
4. 统一机制更优雅，论文解释也更简洁

【计算量评估】（数据集10000条新闻，设备RTX 4060 8GB）
  - 单epoch预计5-10秒，完整训练约20-50分钟
  - 显存占用约2-3GB，远低于8GB上限
  - LLM意图提取是离线预处理，不参与训练循环
  - 结论：完全带得动，不需要为速度牺牲效果

========================================

【议题3】评论网络的构建方式

【结论】在初版基础上新增作者回复

【评论生成策略】
  - 10个虚拟读者评论（保留初版的直接阅读路径 + 间接交互路径）
  - 1-2个作者回复（新增，作者针对读者评论的回应）
  - 作者回复是元路径 C-AC-C 成立的基础

========================================

【议题4】异构图的节点类型

【结论】采用3类节点设计：N（新闻）、A（作者）、C（评论）

【节点设计】
  | 节点类型 | 含义               | 数量级（10000条新闻）|
  | N（新闻）| 被检测的对象         | 10,000              |
  | A（作者）| 新闻发布者           | 几百~几千            |
  | C（评论）| 读者评论 + 作者回复   | ~100,000             |

【边类型】
  - N→C：新闻与其评论的关联
  - A→N：作者发布新闻
  - A→C：作者回复评论
  - C→C：评论之间的互动（回复、相似）

【理由】
  1. 3类节点是5条元路径成立的最低要求
  2. 每类节点语义角色不同（被检测对象/信息源头/传播反馈），独立建模更清晰
  3. 三者特征空间天然不同，独立建模更合理

========================================
